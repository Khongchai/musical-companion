mutation ForgotPassword(
  $token: String!
  $newPassword1: String!
  $newPassword2: String!
) {
  forgotPassword(
    token: $token
    newPassword1: $newPassword1
    newPassword2: $newPassword2
  ) {
    success
    errors
  }
}
